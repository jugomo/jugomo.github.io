name: json_editor
description: A new Flutter project.
publish_to: 'none'
version: 0.6.0+6

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  file_selector: ^1.0.2
  nice_json: ^1.1.1
  file_picker: ^6.1.1
  yaml: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - pubspec.yaml
